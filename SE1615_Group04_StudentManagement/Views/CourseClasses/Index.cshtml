@model IEnumerable<SE1615_Group04_StudentManagement.Models.CourseClass>

@{
    ViewData["Title"] = "Index";
}

<h1>@Model.First().Classid</h1>

<p>
   
    <a asp-action="Create"  asp-route-id="@Model.First().Classid">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Class)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Class.Classid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject.Subjectid)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
